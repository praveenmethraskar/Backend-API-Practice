export default class User {
    constructor(firstname, lastname, exam, address) {
        this.firstname = firstname
        this.lastname = lastname
        this.exam = exam
        this.address = address
    }
}